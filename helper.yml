# Add the "inno setup" entries to the copy files:package folder section
register components:
  - key: inno setup
    kind: copy files
    target platform: package folder
    callback stack: inno_setup_packager.livecodescript # Inno Setup Packager
# Don't include when packaging an application
# include when packaging: false
distribute: false
